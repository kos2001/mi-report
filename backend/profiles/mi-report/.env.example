# Hermes Gateway 프로파일 시크릿 (복사: cp .env.example .env)
# config.yaml 의 providers.hermes-gateway.key_env 가 가리키는 변수.
#
# 값은 Hermes 설치본의 api_server 토큰을 사용한다:
#   ~/.hermes/config.yaml -> platforms.api_server.token
# 또는 `hermes` 실행 환경에서 발급된 게이트웨이 토큰.
HERMES_GATEWAY_API_KEY=
